// Check if variable is non-existing (undefined;uninitlialized;null)
if (typeof variable === 'undefined' || variable === null) {
    // variable is undefined or null
}

// Adapted from: http://stackoverflow.com/a/6509422